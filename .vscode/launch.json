{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch stravaDataExporter",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server",
            "envFile": "${workspaceFolder}/.env",
            "buildFlags": [
                "-buildvcs=false"
            ]
        }
    ]
}